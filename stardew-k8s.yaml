apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-stardew
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend-api
        image: raffienanda/stardew-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        # INI ADALAH IDENTITY INJECTION
        env:
        - name: NAMA_PRAKTIKAN
          value: "Raffie Arsy Ananda"
        - name: NIM_PRAKTIKAN
          value: "2405916"
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
  type: ClusterIP # Tipe service wajib ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-stardew
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend-ui
        image: raffienanda/stardew-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP # Tipe service wajib ClusterIP